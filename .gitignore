node_modules/
dist/
dist-ssr/
.vercel/
*.local

# Secrets. api/.env holds the service-role key and the database URLs.
.env
.env.local
.env.*.local
# These carry nothing secret and are meant to be committed.
!.env.example
!.env.production
# Zipped copy of api/.env (real secrets) — never commit it.
env.zip

.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

.DS_Store
*.log

# Local agent/test helper scripts and docs (not part of the app).
AGENTS.md
*_probe.py
*_diag.py
*_check.py
probe*.py
